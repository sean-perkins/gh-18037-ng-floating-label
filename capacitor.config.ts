import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ng-invalid-floating-label',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
